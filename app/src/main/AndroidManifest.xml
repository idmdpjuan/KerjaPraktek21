<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kpws.kpprojectwarehousestok">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KPProjectWarehouseStok"
        android:usesCleartextTraffic="true">
        <activity android:name=".activitymain.BarangActivity"></activity>
        <activity android:name=".activityall.TambahStokActivity" />
        <activity android:name=".activitymain.StokActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".activityall.UbahStokActivity" />
        <activity android:name=".activitymain.LoginActivity" />
        <activity
            android:name=".activitymain.BakalActivity"
            android:parentActivityName=".activitymain.BamaActivity" />
        <activity android:name=".activitymain.BamaActivity" />
        <activity
            android:name=".activitymain.MainActivity"
            android:parentActivityName=".activitymain.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".scanbarcode.ScanCodeActivity" />
    </application>

</manifest>